import React from 'react';

interface Props {
    updateLatitude: any;
    updateLongitude: any;
}

const MapLocator = (props: Props) => {
    return (
        <div>
            MapLocator
        </div>
    )
}

export default MapLocator;
